/**
 * 04.19.2026 13:35 Score Consolidation
 * @deprecated — file renamed to grade-scale.ts during the same session it was created.
 * This re-export shim exists only to prevent any hypothetical stale import.
 * Import from '$lib/intel/scoring/grade-scale' instead.
 */
export { scoreToGrade } from '$lib/intel/scoring/grade-scale';
